name: 'bus_censor_dbt'
version: '1.0.0'
config-version: 2

profile: 'bus_censor_snowflake'

model-paths: ["models"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  bus_censor_dbt:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts